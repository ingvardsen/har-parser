# Har Parser

This is an Elixir leaning project.

**Simple CLI (Command Line Tool) for extracting network traffic**

Extracting network traffic from HAR (HTTP Archive) file.

Outputs a list of `Status`, `method` and `URL's`, and can be filtered.

## Installation
TBD

## Usage
TBD
